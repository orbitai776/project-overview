info:
  name: get ai service
  type: http
  seq: 2

http:
  method: GET
  url: "{{host-gateway}}/v1/api/partner/ai-services"
  headers:
    - name: Authorization
      value: " Bearer {{token-user}}"
  body:
    type: json
    data: |-
      {
        "service_id": "44bf64e7-e4e6-4486-baeb-531a7da1967a"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
