info:
  name: get list ai service
  type: http
  seq: 3

http:
  method: GET
  url: "{{host-gateway}}/v1/api/partner/ai-services-all"
  headers:
    - name: Authorization
      value: " Bearer {{token-user}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
