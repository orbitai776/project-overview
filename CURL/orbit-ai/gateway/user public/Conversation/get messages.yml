info:
  name: get messages
  type: http
  seq: 3

http:
  method: GET
  url: "{{host-gateway}}/v1/api/chat/conversations/69e46764ce2fec332b358cd7/messages"
  headers:
    - name: Authorization
      value: " Bearer {{token-user}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
