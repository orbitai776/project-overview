info:
  name: get conversation
  type: http
  seq: 1

http:
  method: GET
  url: "{{host-gateway}}/v1/api/chat/conversations"
  headers:
    - name: Authorization
      value: " Bearer {{token-user}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
