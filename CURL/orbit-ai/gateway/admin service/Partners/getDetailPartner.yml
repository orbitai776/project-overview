info:
  name: getDetailPartner
  type: http
  seq: 2

http:
  method: GET
  url: "{{host-gateway}}/v1/api/admin/partners/b82612c5-498d-4167-b9a4-577493b2c7ca"
  headers:
    - name: Authorization
      value: " Bearer {{token-admin}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
