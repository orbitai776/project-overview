info:
  name: HistoryToken
  type: http
  seq: 3

http:
  method: GET
  url: "{{host-gateway}}/v1/api/admin/partners/e4a926ad-d5c6-471a-917f-d0126d1730c3/tokens/payments"
  headers:
    - name: Authorization
      value: " Bearer {{token-admin}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
