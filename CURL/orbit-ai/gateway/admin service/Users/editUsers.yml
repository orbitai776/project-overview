info:
  name: editUsers
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host-gateway}}/v1/api/admin/users/7d9b2f17-424a-4c15-8212-1a6edd9a12bb"
  headers:
    - name: Authorization
      value: Bearer {{token_user}}
  body:
    type: json
    data: |-
      {
        "status": "active"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
