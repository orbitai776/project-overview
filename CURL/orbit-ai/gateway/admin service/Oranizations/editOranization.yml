info:
  name: editOranization
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host-gateway}}/v1/api/admin/partners/organizations/e4a926ad-d5c6-471a-917f-d0126d1730c3"
  headers:
    - name: Authorization
      value: " Bearer {{token-admin}}"
  body:
    type: json
    data: |-
      {
        "name": "Công ty ABCfwfwef",
        "email": "abcfwefe34@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
