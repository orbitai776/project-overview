info:
  name: getOranizationById
  type: http
  seq: 1

http:
  method: GET
  url: "{{host-gateway}}/v1/api/admin/organizations/e4a926ad-d5c6-471a-917f-d0126d1730c3"
  headers:
    - name: Authorization
      value: " Bearer {{token-admin}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
