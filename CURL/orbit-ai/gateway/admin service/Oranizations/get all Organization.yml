info:
  name: get all Organization
  type: http
  seq: 3

http:
  method: GET
  url: "{{host-backend-netcore}}/v1/api/admin/organizations"
  headers:
    - name: Authorization
      value: " Bearer {{token-admin}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
