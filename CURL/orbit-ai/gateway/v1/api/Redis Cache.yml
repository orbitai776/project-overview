info:
  name: Redis Cache
  type: http
  seq: 1

http:
  method: GET
  url: "{{host-gateway}}/v1/api/auth/redis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
