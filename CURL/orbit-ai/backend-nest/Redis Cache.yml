info:
  name: Redis Cache
  type: http
  seq: 2

http:
  method: GET
  url: "{{host-backend-nest}}/public/auth/redis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
